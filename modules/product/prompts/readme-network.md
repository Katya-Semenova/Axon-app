# Промт: README — точки входа в сервис

> Универсальный модуль библиотеки. Происхождение: курс vibecoding, урок 07 (обезличено 2026-07-04).

> **Цель:** у сервиса есть корневой `README.md` (что за продукт, как запустить, карта документации) и короткие `README.md` в ключевых папках (`docs/`, `src/` приложения, `.claude/`, `modules/`, `packages/ui/`). Каждый README — указатель «что здесь и куда идти за деталями», а не копия спеки.

---

## Зачем это (прочитай сам перед запуском)

README — это первая дверь в папку. Когда в репозиторий заходит новый человек или AI открывает незнакомую папку, README — первое, что читают. Без него каждый раз приходится заново объяснять «что это, с чего начать, куда смотреть».

- **Для человека.** Вернулись к проекту через 2 месяца — README за минуту напоминает, как запустить и где что лежит. Пришёл второй разработчик — не дёргает вас по каждому «а это что за папка».
- **Для AI.** Перед тем как трогать файлы в папке, AI читает её README. Это снижает число ошибок: он не выдумывает структуру заново и не кладёт файл не туда. README в папке работает как локальная инструкция — дешевле, чем держать всё в одном большом `CLAUDE.md`.
- **README ≠ spec.** Спека описывает поведение продукта. README — это карта-указатель: что лежит в этой папке, зачем она нужна, как её менять и куда идти за деталями (ссылки на `spec.md`, `screens/`, `memory/INDEX.md`). Никакого дублирования — только навигация.

Принцип: **в каждой значимой папке — свой короткий README.** Тогда и человек, и AI входят в любую часть сервиса через понятную дверь.

---

## Промт

```
Собери сеть README по сервису: корневой README.md + короткие README.md в ключевых папках.
Каждый README — указатель (что здесь, зачем, куда идти за деталями), НЕ копия spec.md.

Сначала прочитай фактическое состояние:
- package.json (workspace) и apps/*/package.json (если монорепо) — имя, скрипты (dev/build/start), стек
- docs/spec.md, docs/app-map.md, docs/screens/ — что за продукт и какие экраны
- структуру кода: например, apps/app (сервис, FSD), apps/landing (лендинг), packages/ui (общие токены/компоненты) — или просто src/, если не монорепо
- .claude/ — память, хуки, правила
- modules/ — личная библиотека (если собрана)

Затем создай / обнови файлы. Держи каждый коротким (15–40 строк),
ссылки относительные и кликабельные. Не выдумывай — бери из кода и docs.

--- 1. Корневой README.md ---

# <Product Name>

<Одно-два предложения: что это за продукт и для кого>

## Live
<публичный URL, если задеплоен>

## Стек
- Frontend: <из package.json>
- Backend: <...>
- DB: <...>
- Hosting: <...>

## Как запустить локально
\`\`\`bash
npm install
npm run dev        # http://localhost:3000
\`\`\`

## Структура
- `src/` — код приложения (см. src/README.md)
- `docs/` — спецификация и тест-кейсы (см. docs/README.md)
- `.claude/` — память проекта, правила, хуки (см. .claude/README.md)
- `modules/` — личная библиотека промтов (см. modules/README.md)

## Документация
- `docs/spec.md` — полная спецификация
- `docs/app-map.md` — карта экранов и маршрутов
- `docs/test-cases.md` — тест-кейсы
- `docs/memory/INDEX.md` — память проекта (ADR, sessions)

--- 2. docs/README.md ---
Коротко: что за документы лежат в папке и в каком порядке читать.
Таблица: файл → что описывает (spec.md, app-map.md, screens/, test-cases.md, audits/).

--- 3. src/README.md ---
Коротко: архитектура кода. Если FSD — объясни слои (app / pages / widgets /
features / entities / shared) и правило «куда что класть».
Если не FSD — опиши фактические папки. Добавь правило про UI-кит:
новые компоненты — из shared/ui, не хардкодить цвета/размеры (см. DESIGN.md).

--- 4. .claude/README.md ---
Коротко: как устроена AI-инфраструктура проекта.
- memory/ — память (INDEX.md — вход, decisions/ — ADR, sessions/ — журнал)
- hooks/ — что делает каждый хук
- rules/ — какие правила и когда срабатывают
- state.md — текущее состояние работы

--- 5. modules/README.md (если папка есть) ---
Коротко: личная библиотека промтов. Вход — modules/_INDEX.md.
Как переносить в новый проект.

--- 6. shared/ui/README.md (если папка есть) ---
Коротко: UI-кит. Список основных компонентов, правило
«экран собирается из этих компонентов, а не из сырых div + классов».

ПОКАЖИ МНЕ:
- Список созданных/обновлённых README
- Корневой README.md целиком
- Открой его в Markdown Preview — проверь, что все ссылки кликаются
```

---

## Edge cases

- **README уже есть.** Не затирай вслепую — дополни недостающие разделы, сохрани то, что человек писал руками.
- **Папки нет** (`modules/`, `shared/ui/`) — пропусти соответствующий README, не создавай пустышку.
- **Монорепо / несколько сервисов** — корневой README описывает общую раскладку, плюс свой README на каждый сервис.
- **Приватные данные** — в README не должно быть секретов, токенов, паролей. Только публичная навигация.
- **Не дублируй spec.** Если тянет переписать в README поведение экрана — остановись и поставь ссылку на `docs/screens/<screen>.md`.
