# Модуль product — карта

Продуктовый этап: интервью по концепции, детализация спеки (Figma / скриншоты / с нуля), финальная сверка + PRD, карта приложения, бриф лендинга, сеть README.

Полный реестр промтов модуля с триггерами «когда звать» — в общем индексе:
`modules/_INDEX.md`, раздел «product». Промт без строки там — «слепой» (AI не найдёт);
проверка: `bash scripts/check-modules-indexed.sh`. Пополнение — скилл `/extend-library`.
