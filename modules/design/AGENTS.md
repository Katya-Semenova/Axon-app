# Модуль design — карта

Визуальный слой: токены DESIGN.md, UI-кит и витрина, Figma Code Connect, контракты и генерация экранов, адаптив- и UX-аудиты, лендинг, скрытие dev-страниц.

Полный реестр промтов модуля с триггерами «когда звать» — в общем индексе:
`modules/_INDEX.md`, раздел «design». Промт без строки там — «слепой» (AI не найдёт);
проверка: `bash scripts/check-modules-indexed.sh`. Пополнение — скилл `/extend-library`.
