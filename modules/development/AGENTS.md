# Модуль development — карта

Код целиком: каркас проекта и FSD, БД/миграции, auth и его защита, письма, файлы, тесты и смоуки, git/деплой/VPS/домен, security-аудиты и гейты, хуки-guardrail'ы, монорепо, ИИ-фичи, админка, REST API, мониторинг.

Полный реестр промтов модуля с триггерами «когда звать» — в общем индексе:
`modules/_INDEX.md`, раздел «development». Промт без строки там — «слепой» (AI не найдёт);
проверка: `bash scripts/check-modules-indexed.sh`. Пополнение — скилл `/extend-library`.
