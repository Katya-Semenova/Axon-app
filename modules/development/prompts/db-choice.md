# Выбор базы данных — Supabase (managed) vs свой Postgres — ADR «слой данных»

> Универсальный модуль библиотеки. Происхождение: курс vibecoding, урок 04 (обезличено 2026-07-04).

> **Цель:** до выбора ORM и до поднятия БД определить, ГДЕ живёт база: готовая платформа (Supabase) или свой Postgres на вашем VPS. Решение фиксируется в ADR «слой данных», туда же потом ляжет выбор ORM.

## Зачем этот шаг

Весь дальнейший бэкенд-этап строится на базе данных. Но сначала надо решить не «какой ORM», а «где вообще живёт база»: на готовой платформе вроде Supabase или на своём сервере. Это та же развилка, что «Vercel vs свой VPS», только про данные. Выбор влияет на цену, контроль, юрисдикцию (152-ФЗ) и на то, делаем ли мы Auth сами или берём готовый.

Важно: Supabase внутри — это тот же PostgreSQL. Поэтому ORM (Prisma/Drizzle) и схема работают одинаково с обоими вариантами, меняется только то, куда указывает `DATABASE_URL` и кто управляет сервером БД.

---

## Промт

```
Помоги выбрать, где будет жить база данных продукта: Supabase (managed) или свой
PostgreSQL на нашем VPS. Объясни разницу и сравни под мой проект.

Сравнительная таблица:

| | Supabase (managed) | Свой Postgres (наш VPS) |
|---|---|---|
| Запуск | проект за пару минут, БД готова сразу | сервис db в docker-compose, чуть дольше |
| Что входит | Postgres + Auth + Storage + Realtime + авто-API | только Postgres (Auth/Storage делаем сами) |
| Цена на росте | бесплатный tier, дорожает с строками/трафиком | стабильно дёшево (тот же VPS, $5-30/мес) |
| Контроль | платформа управляет БД за вас | база полностью ваша |
| Данные / 152-ФЗ для РФ | серверы вне РФ (US/EU) | данные на вашем сервере, можно РФ |
| Бэкапы | автоматические (на платных тарифах) | свой cron-бэкап (настроим на шаге поднятия БД) |
| Привязка | завязка на Supabase (хотя внутри Postgres) | переносится на любой сервер |

Поясни ключевую развилку — она про данные, не про удобство: у Supabase база и
данные пользователей лежат на стороне платформы (вне РФ), у своего Postgres — на
вашем VPS, где вы их контролируете и держите в нужной юрисдикции. Поэтому:
- если строите продукт с данными пользователей под РФ-аудиторию (152-ФЗ) или
  хотите полный контроль и предсказуемую цену — свой Postgres;
- если нужен быстрый старт, аудитория по миру, и БД + Auth + Storage из коробки
  ценнее контроля — Supabase.

По умолчанию рекомендуемый путь — свой Postgres (тот же VPS, где задеплоен
продукт): данные под контролем, дешевле на росте, подходит под РФ, и вы понимаете,
как устроена база. Но это не «единственно правильно» — если ваш случай за
Supabase, это нормально.

Добавь короткий контекст про managed-базу на будущее (без смены дефолта): managed-базу
(PostgreSQL, которым управляет провайдер) можно взять не только у Supabase, но и у
российских провайдеров (Yandex Cloud, Selectel, VK Cloud, Timeweb) — данные остаются в
РФ (152-ФЗ). Отличие от своего Postgres: провайдер сам делает бэкапы, обновления и держит
резервный сервер, а вы платите отдельно. На старте это не критично — свой Postgres дешевле
и под контролем. Брать managed имеет смысл на росте, когда данные критичны и нужна
максимальная надёжность. (Про файлы/S3 — отдельно в `file-storage.md`, здесь не трогаем.)

Если я выберу Supabase — сразу предупреди, что меняется дальше по этапу:
- Шаг «Postgres в docker-compose» пропускается: вместо этого берём строку
  подключения из дашборда Supabase и кладём в DATABASE_URL (.env);
- Auth можно взять готовый от Supabase вместо better-auth (шаг auth) — отметь это
  как развилку, чтобы я решил позже;
- ORM (Prisma, см. ниже) работает с Supabase так же, как со своим Postgres.

ORM не выбираем отдельно, чтобы не отвлекать пользователя: по умолчанию берём
**Prisma** (популярный, удобный язык схемы, у AI больше всего примеров) — он
ставится автоматически на шаге со схемой. Скажи об этом одной строкой и запиши в
ADR как принятый дефолт. Если пользователь сам захочет рассмотреть альтернативу
(Drizzle) — есть опциональный промт orm-choice.md.

Спроси меня про базу, и зафиксируй решение в
docs/memory/decisions/ADR-XXX-data-layer.md:

# ADR-XXX: Слой данных — база и ORM

## Context
Нужно решить, где живёт БД (Supabase managed vs свой Postgres на VPS). ORM берём
по умолчанию (Prisma), чтобы не усложнять. Продукт — <описание из spec.md>,
аудитория — <откуда>.

## Decision
### База данных
<Supabase | Свой Postgres на VPS> — потому что <причина: контроль/152-ФЗ/цена/
скорость старта>.

### ORM
Prisma (дефолт — прикручен автоматически, SQL руками не пишем). Альтернатива
Drizzle не выбиралась осознанно; при необходимости — отдельный ADR позже.

## Alternatives Considered
### <вариант, который НЕ выбрали для БД>
- Плюсы: ...
- Минусы / почему не выбрали: ...

## Consequences
- DATABASE_URL указывает на <docker-сервис db | Supabase connection string>.
- <если Supabase: шаг docker-compose пропускаем; возможен Supabase Auth вместо better-auth.>
- <если свой Postgres: поднимаем db в docker-compose, свой бэкап, Auth через better-auth.>

## Status
Accepted

## Date
<сегодняшняя дата YYYY-MM-DD>

ПОСЛЕ:
- Добавь строку в docs/memory/INDEX.md (таблица ADR).
- Покажи мне созданный ADR и краткий вывод: какую БД выбрали и почему.
```

---

## Edge cases

- Supabase = PostgreSQL внутри, поэтому переезд «Supabase → свой Postgres» позже возможен (дамп → восстановление), но обратной дороги от vendor-фич (Realtime, RLS-политики, Edge Functions) уже сложнее. Если завязались на фичи Supabase — это и есть привязка.
- Если выбрали Supabase, но позже всё равно нужен 152-ФЗ — придётся переезжать на свой Postgres. Лучше решить это сейчас, по аудитории и типу данных.
- Бесплатный tier Supabase ставит проект на «паузу» при простое и ограничивает строки/трафик — для пет-проекта ок, для боевого продукта считайте цену на росте.
- Этот ADR общий с выбором ORM (следующий промт). БД и ORM — две части одного решения «слой данных», поэтому живут в одном `ADR-XXX-data-layer.md`.
