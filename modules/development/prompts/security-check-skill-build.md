# Первый ручной аудит безопасности + сборка своего скилла `/security-check`

> Универсальный модуль библиотеки. Происхождение: курс vibecoding, урок 03 (обезличено 2026-07-04).

> **Цель:** прогон чек-листа безопасности после первого выкатывания на VPS — **руками**, по шагам, чтобы понять что именно проверяется. В конце — оформляешь это как свой `prompts/security-check.md` + скилл-обёртку, чтобы потом не повторять руками.

> **Когда:** после фиксации инфраструктуры (промт infrastructure-snapshot.md) — у тебя на сервере поднят docker-compose, есть HTTPS, базовая безопасность, hooks-guardrails. Сейчас проверяешь что ничего не упустил.

---

## Зачем «руками + потом промт», а не «сразу `/security-check`»

Получить готовый `/security-check` сразу — быстро, но ты его не понимаешь и применяешь как магию. Пройти аудит руками сейчас, понять что проверяется и почему — и **только потом** оформить как промт — это твой реальный навык безопасности.

К финальному security-check перед публичным запуском у тебя уже есть твой собственный `/security-check`, ты его прогнал на этапах деплоя, auth и расширений — это твой инструмент.

---

## Промт

```
Проведи со мной первый ручной аудит безопасности по 14 разделам OWASP-style.
В конце — поможем оформить это как мой собственный промт /security-check.

КОНТЕКСТ:
- Этап деплоя завершён: сервис на VPS, HTTPS, docker-compose, nginx, базовая безопасность
  (UFW, fail2ban, SSH-ключи), hooks в .claude/hooks/
- Auth ещё нет, фичи на моках. Поэтому часть разделов помечаем N/A
- Эталон-шаблон: security-check-template.md (в библиотеке модулей) — используй
  как ОРИЕНТИР, не зачитывай мне как лекцию

ШАГ 1 — Прогон по 14 разделам (только релевантные на этом этапе).

🟢 ОБЯЗАТЕЛЬНО на этом этапе:

  L. Deployment hardening:
     - Контейнеры запущены НЕ от root (USER директива в Dockerfile)?
     - .env.production имеет права 600 (chmod, owner — не www-data)?
     - UFW открыты только нужные порты (22 SSH, 80, 443)?
     - SSH только по ключам, не по паролю (PasswordAuthentication no)?
     - fail2ban активен, ловит SSH-перебор?
     - Бэкапы БД и .env в безопасном месте (не в репо)?

  H. HTTP security headers (nginx config):
     - Content-Security-Policy установлен?
     - X-Frame-Options: DENY (защита от clickjacking)?
     - X-Content-Type-Options: nosniff?
     - Referrer-Policy: strict-origin-when-cross-origin?
     - Strict-Transport-Security (HSTS) если HTTPS работает стабильно?

  G. Secrets:
     - .env.production НЕТ в git (.gitignore + git log проверка)?
     - Hooks .claude/hooks/pre-tool-use-write.sh ловит попытки записать секрет
       в код?
     - Старых секретов в коммитах нет (git log -p | grep -E 'sk-|ghp_|AKIA')?

  I. Body size & timeouts:
     - nginx client_body_timeout установлен (защита от slowloris)?
     - client_max_body_size (для будущих uploads — задай разумный лимит)?

🟡 ЧАСТИЧНО — пройдись, помечай где «пока заглушка»:

  A. Authorization: auth ещё нет, но проверь что чувствительные роуты
     (если они есть как заглушки) хотя бы помечены как «требует логина»

  M. Production-конфиг next.config:
     - ignoreBuildErrors НЕ установлен в true?
     - eslint в build не отключён?

⚪ N/A — отложено до следующих этапов:

  B. Inputs & Injection (БД ещё нет — этап работы с данными)
  C. XSS (фичи на моках — этап расширений)
  D. CSRF (этап Auth)
  E. Authentication / Sessions (этап Auth)
  F. File uploads (этап расширений)
  J. Webhooks / Payments (этап расширений)
  K. Rate limiting (поверхностно сейчас, детально — после Auth)
  N. Архитектурные ловушки (специфично, по мере роста)

ВАЖНО: для каждого 🟢 пункта — проведи реальную проверку (curl / ssh-команда /
посмотри файл). Не «теоретически да». Если что-то не так — зафиксируй как
🔴 (критично, чинить сейчас) или 🟡 (можно отложить, но в backlog).

ШАГ 2 — Сохрани отчёт.

В docs/audits/security-check-<YYYY-MM-DD>.md:
- Таблица: раздел / статус (✅ / 🟡 / 🔴 / N/A) / что проверял / что нашёл
- Список 🔴 проблем — починить ДО следующего этапа
- Список 🟡 — в backlog docs/memory/findings/

ШАГ 3 — Почини 🔴 проблемы (если есть).

По каждой 🔴:
- Конкретное действие (chmod, правка nginx, удалить лишнее из git history)
- Перепроверка после исправления

ШАГ 4 — Теперь собираем мой промт.

Всё что мы только что прошли — это мой будущий /security-check. Оформляем.

Прочитай методичку сборки промтов (prompts/_HOW-TO-BUILD-YOUR-PROMPT.md, если
она есть в проекте). По 6 секциям — спрашивай по ОДНОМУ вопросу за раз:

1. **Когда применять**:
   - После каждого деплоя на новом сервере (как сейчас)
   - После добавления нового вектора атаки (uploads, Auth и т.п.)
   - Перед публичным запуском продукта (финальный полный аудит)
   - Месячно на проде через cron
   - Спроси меня — что добавить из моего опыта

2. **Параметры**:
   - URL прода, доступ по SSH
   - Какие разделы проверяем (зависит от стадии: после деплоя — другой набор
     чем после расширений)
   - Уровень глубины (быстрый / полный)

3. **Шаги**:
   - Сначала прочитать docs/security-checklist.md — растущий список
     «что проверять» (его заводим здесь; дальше этапы БД/auth, расширений и
     финала дописывают новые векторы). Каждый прогон сверяется с ним целиком,
     чтобы ничего не пропустить.
   - 14 разделов OWASP-style (но в твоей формулировке — только что прошёл)
   - Шаблон отчёта docs/audits/
   - Где чинить 🔴, что в backlog 🟡 (docs/security-todo.md — это
     бэклог находок, не путать с checklist «что проверять»)

4. **Edge cases**:
   - Если HTTPS ещё не настроен — пропусти HSTS
   - Если на staging — другие правила (debug ok, чужие test-данные ok)
   - Если в инциденте — приоритет на разделы C, D, E

5. **Что должно получиться**:
   - docs/audits/security-check-<YYYY-MM-DD>.md с таблицей раздел × статус
   - 🔴 = 0 как критерий запуска

6. **Триггеры активации**:
   - "проверь безопасность"
   - "есть ли уязвимости"
   - "security check" / "OWASP" / "аудит безопасности"
   - "проверь auth" / "проверь IDOR" / "проверь secrets"

ШАГ 5 — Сохрани в проекте.

prompts/security-check.md — твой собственный промт

Эталон-шаблон (security-check-template.md) использовать
как ориентир структуры. Минимум одна твоя правка относительно эталона:
- Своя формулировка приоритетов (или тот же 🟢/🟡/⚪)
- Свой шаблон отчёта
- Свои edge cases из реального аудита (что нашёл сейчас на проде)

ШАГ 6 — Оформи инструмент так, чтобы он запускался сам по ключевым словам.

Цель одна для всех AI: чтобы /security-check срабатывал не только когда ты явно
позвал его по пути, но и сам — когда пользователь пишет «проверь безопасность»,
«нет ли уязвимостей» и т.п., а также перед деплоем. Механизм разный:

  • Claude Code → скилл-обёртка с триггерами в `description` (ниже).
  • Codex / Cursor → скиллов нет, поэтому собираем аналог из этого же промта:
    рецепт `prompts/security-check.md` (из ШАГА 5) уже готов, осталось научить
    инструмент звать его сам. Добавь в `AGENTS.md` правило-триггер:

        ## Security check — авто-запуск
        Если пользователь пишет «проверь безопасность» / «есть ли уязвимости» /
        «security check» / «аудит безопасности», ИЛИ перед каждым деплоем
        (./deploy.sh) — прочитай `prompts/security-check.md` и выполни. Найденные
        🔴 чини до коммита/деплоя.

    Codex/Cursor читают AGENTS.md в начале каждой сессии — это и есть их
    «авто-триггер по ключевым словам» вместо скилла. Папка .claude/ и ШАГ 7
    (тест скилла) тебе не нужны; вместо ШАГА 7 просто проверь, что правило
    в AGENTS.md на месте.

Если ты Claude Code — в .claude/skills/security-check/SKILL.md создай обёртку:

---
name: security-check
description: Полный аудит безопасности веб-приложения по 14 разделам OWASP-style (auth/IDOR, XSS, CSRF, injections, file uploads, secrets, HTTP headers, webhooks, deployment hardening). Триггеры: "проверь безопасность", "есть ли уязвимости", "security check", "OWASP", "аудит безопасности".
model: opus
---

# /security-check

Обёртка над промтом `prompts/security-check.md`.

## Триггеры
- "проверь безопасность"
- "есть ли уязвимости"
- "security check" / "OWASP" / "аудит безопасности"
- "проверь auth" / "проверь IDOR" / "проверь secrets"

См. полный промт → [prompts/security-check.md](../../prompts/security-check.md)

ШАГ 7 — Тестовый прогон.

Скажи AI фразу-триггер ("проверь безопасность" или "запусти security check").
Должен автоматически распознать скилл и применить промт.

ШАГ 8 — Зафиксируй в state.md.

В docs/memory/state.md в раздел "Готово":
- "Прошёл первый ручной аудит безопасности — отчёт в docs/audits/"
- "Собран свой /security-check промт — детали в prompts/security-check.md"

ПОКАЖИ МНЕ:
- docs/audits/security-check-<YYYY-MM-DD>.md
- prompts/security-check.md (мой собственный) — главное, нужно всем
- Если ты Claude Code: .claude/skills/security-check/SKILL.md и `ls .claude/skills/`
  (обёртка-скилл). Если Codex/Cursor — этого пункта нет, рецепт = промт выше
- Краткий отчёт о тестовом прогоне (на Claude — через триггер скилла; на
  Codex/Cursor — вызвав prompts/security-check.md по пути)
```

---

## Что должно получиться

```
my-service/
├── docs/audits/
│   └── security-check-<YYYY-MM-DD>.md   ← первый отчёт
├── prompts/
│   ├── README.md
│   ├── _HOW-TO-BUILD-YOUR-PROMPT.md
│   ├── save-session.md
│   ├── memory-search.md
│   ├── new-feature.md
│   ├── ux-testing.md
│   └── security-check.md     ← ТВОЙ, собрал сейчас
└── .claude/skills/    ← только Claude Code (обёртки; Codex/Cursor эту папку не делают — у них рецепты = промты выше)
    ├── save-session/SKILL.md
    ├── memory-search/SKILL.md
    ├── new-feature/SKILL.md
    ├── ux-testing/SKILL.md
    └── security-check/SKILL.md   ← ТВОЯ обёртка
```

## Связь со следующими этапами

После сборки твоего `/security-check`:
- **Auth** — после настройки Auth прогоняешь свой `/security-check` с фокусом на разделах A, D, E (новый вектор: сессии, авторизация, IDOR)
- **Расширения** — после uploads, webhooks, AI — фокус на F, J, B (новый вектор: file uploads, payments, AI input sanitization)
- **Финал** — финальный полный прогон по всем 14 разделам перед публичным запуском + настройка месячного cron на проде

## Edge cases

- Если в текущем аудите найдено 🔴 5+ проблем — это сигнал что на этапе деплоя ты пропустил настройку безопасности. Возвращайся к промтам безопасности сервера, домена/HTTPS и hooks (server-security-basics, domain-https-nginx, ai-guardrail-hooks), починяй, потом делай аудит заново
- Если ты не понимаешь какой-то пункт (например, что такое CSP) — попроси AI объяснить ДО проверки. Не вписывай ✅ если не понимаешь
- Если эталон-шаблон слишком длинный для твоего уровня — собирай свой промт короче. Лучше короткий и понятный, чем длинный и непрочитанный

## Антипаттерны

- 🔴 «Делегировал AI всё, не понял что проверял» — теряешь смысл этапа. Шаг 1 (ручной прогон) — должен быть **твоим**, ты должен видеть что чем проверяется
- 🔴 Скопировать эталон-шаблон 1-в-1 — это не твой промт. Минимум одна твоя правка в шагах или edge cases
- 🔴 Пропустить шаг 3 (починка 🔴) — security-check без починки бесполезен. Не переходи к шагу 4 пока не закрыл критичные проблемы
