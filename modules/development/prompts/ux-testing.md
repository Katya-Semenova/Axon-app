# Собери свой скилл `/ux-testing`

> Универсальный модуль библиотеки. Происхождение: курс vibecoding, урок 07 (обезличено 2026-07-04).

> **Цель:** в `prompts/ux-testing.md` твоего проекта лежит твой собственный промт для UX-тестирования через браузер. Это твой собственноручно собранный рецепт — основа для будущих проектов. На Claude Code его дополнительно оборачивают в скилл `.claude/skills/ux-testing/SKILL.md` (вызов `/ux-testing`); на Codex/Cursor рецепт — это сам промт.

> **Когда:** после прохождения адаптива (320/768/1024/1440 px) и состояний empty/loading/error на прототипе. Ты только что прогнал свой UI и зафиксировал состояния — это и есть паттерн UX-тестирования. Сейчас оформляешь его в переиспользуемую методичку.

---

## Зачем оформлять сразу, а не «когда понадобится»

Получить готовый `/ux-testing` со стороны — быстро, но ты его не понимаешь и не используешь как свой. Оформить **то, что только что прошёл руками** — это твой реальный промт, под твою манеру и твой стек. Так постепенно собирается личный переносимый AI-стек: несколько промтов, каждый из которых ты собрал сам.

---

## Промт

```
Помоги мне оформить переиспользуемый промт /ux-testing на основе того,
что мы только что делали (адаптив + состояния).

КОНТЕКСТ:
- Только что я прогнал UI сервиса на 320 / 768 / 1024 / 1440 px
- Зафиксировал состояния empty / loading / error на всех экранах
- Это паттерн, который повторится: финальный регрессионный аудит,
  любой будущий проект после релиза, при добавлении новой страницы
- Эталон-шаблон: prompts/ux-testing.md
  (используй как ОРИЕНТИР структуры, не копируй буквально)

ШАГ 1 — Прочитай методичку.

Прочитай prompts/how-to-build-your-prompt.md (модуль ai-config) — там полный фрейм
из 6 секций (Когда применять / Параметры / Шаги / Edge cases / Что должно
получиться / Триггеры активации).

ШАГ 2 — Соберём структуру.

По каждой из 6 секций — спрашивай меня по ОДНОМУ вопросу за раз, не списком.

Стартовые наброски на основе моего опыта:

1. **Когда применять**:
   - После добавления новой страницы — проверка адаптива
   - Перед публичным запуском — финальный регрессионный аудит
   - Если "что-то сломалось на телефоне" — диагностический прогон
   - Спроси меня — какие ситуации добавить

2. **Параметры**:
   - URL для проверки (localhost:3000 или прод)
   - Список страниц / маршрутов
   - Список разрешений (по умолчанию 320 / 768 / 1024 / 1440)
   - Спроси меня — что ещё нужно знать

3. **Шаги**:
   - Запустить браузер (Playwright или вручную в DevTools)
   - Пройти по каждой странице из списка
   - На каждой — четыре разрешения, по очереди
   - Зафиксировать состояния: empty, loading, error
   - Сделать скриншоты в .tmp-ux/ если есть проблемы
   - Сформировать отчёт docs/audits/ux-audit-<YYYY-MM-DD>.md
   - Спроси меня — какие шаги добавить или переформулировать

4. **Edge cases**:
   - Если страница требует логина — указать тестовый аккаунт
   - Если в проде — отдельная мокированная учётка для аудита
   - Спроси меня — что я хочу подсветить из своего опыта

5. **Что должно получиться**:
   - Файл docs/audits/ux-audit-<YYYY-MM-DD>.md с табличкой страница × разрешение × состояние
   - Скриншоты проблем в .tmp-ux/
   - Список задач на починку (если есть)
   - Спроси меня — что я хочу видеть в финальном отчёте

6. **Триггеры активации** (для скилла Claude Code):
   - "протестируй UX"
   - "пройди путь пользователя"
   - "адаптивный аудит"
   - "регрессионный прогон"
   - Спроси меня — какие фразы я использую естественно

ШАГ 3 — Сохрани в моём проекте.

Положи файл в prompts/ux-testing.md.

Если эталон-шаблон содержит полезные части — покажи мне 2-3 варианта
формулировки, я выберу или поправлю.

Минимум одна МОЯ правка относительно шаблона: переформулировка шага, своё edge
case, свои триггеры. Иначе это не "мой промт", а копия.

ШАГ 4 — Обёртка в скилл. ТОЛЬКО Claude Code.

Если ты Codex / Cursor / другой AI — ШАГ 4 и ШАГ 5 ПРОПУСТИ. Твой готовый
рецепт это prompts/ux-testing.md; дальше вызывай его по пути: «прочитай
prompts/ux-testing.md и выполни». Никакой обёртки и .claude/ тебе не нужно.

Если ты Claude Code — в .claude/skills/ux-testing/SKILL.md создай обёртку:

---
name: ux-testing
description: UX-тестирование через браузер. Прогон сценариев, адаптивный аудит на 320/768/1024/1440, состояния empty/loading/error, регрессии. Применять, когда пользователь говорит "протестируй UX", "пройди путь пользователя", "проверь адаптив", "адаптивный аудит".
model: sonnet
---

# /ux-testing

Обёртка над промтом `prompts/ux-testing.md`.

## Триггеры
- "протестируй UX"
- "пройди путь пользователя"
- "адаптивный аудит"
- "регрессионный прогон"

См. полный промт → [prompts/ux-testing.md](../../prompts/ux-testing.md)

ШАГ 5 — Тестовый прогон.

Я скажу тебе фразу-триггер ("прогони адаптивный аудит" или похожую). Ты должен
автоматически распознать триггер, найти скилл /ux-testing и применить мой промт.

Если не распознал — поправь description / триггеры в SKILL.md и попробуй ещё раз.

ШАГ 6 — Зафиксируй в state.md.

В docs/memory/state.md в раздел "Готово" (или "Активный бэклог" если ещё не закрыт)
добавь строку:
"Собран свой /ux-testing промт — детали в prompts/ux-testing.md"

ПОКАЖИ МНЕ:
- prompts/ux-testing.md (мой собственный) — главное, нужно всем
- Если ты Claude Code: .claude/skills/ux-testing/SKILL.md и `ls .claude/skills/`
  (обёртка-скилл). Если Codex/Cursor — этого пункта нет, рецепт = промт выше
- Краткий отчёт о тестовом прогоне (на Claude — через триггер скилла; на
  Codex/Cursor — вызвав prompts/ux-testing.md по пути)
```

---

## Что должно получиться

```
my-service/
├── prompts/
│   ├── README.md
│   ├── how-to-build-your-prompt.md
│   ├── save-session.md
│   ├── memory-search.md
│   ├── new-feature.md
│   └── ux-testing.md         ← ТВОЙ, собрал сейчас
└── .claude/skills/    ← только Claude Code (обёртки; Codex/Cursor эту папку не делают — рецепты = промты выше)
    ├── save-session/SKILL.md
    ├── memory-search/SKILL.md
    ├── new-feature/SKILL.md
    └── ux-testing/SKILL.md   ← ТВОЯ обёртка
```

После этого Claude автоматически подхватывает `/ux-testing` по триггерам — при финальном регрессионном аудите ты уже не пишешь длинную инструкцию, просто говоришь «прогони адаптивный аудит» и работа идёт по твоему промту.

---

## Edge cases

- Если ты не использовал Playwright и прогнал руками в DevTools — это нормально, в шагах промта укажи оба варианта (автоматический через Playwright и ручной)
- Если адаптив у тебя был только на 320 / 1024 (без 768 и 1440) — оставь то что было, не добавляй разрешения которыми не пользовался. Промт должен отражать твою реальную практику
- Если хочешь сначала закрыть сессию (`/save-session`), а потом собрать промт — нормально, главное собрать по свежим следам, не оставлять на потом

---

## Антипаттерны

- 🔴 Скопировать эталон-шаблон 1-в-1 — это не твой промт. Минимум одна твоя правка
- 🔴 Промт на 2-3 строки — это не промт. Если описание реально короткое — пиши в чат, не оформляй
- 🔴 Скилл без триггеров в `description` — Claude не распознает автоподхват. Минимум 3-5 фраз
