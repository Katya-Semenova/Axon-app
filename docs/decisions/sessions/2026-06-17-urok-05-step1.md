# Сессия 2026-06-17 — Урок 5, Шаг 1 (ИИ в продукте)

## Что сделано
Провайдер-агностичный ИИ-слой `lib/ai/` (дефолт OpenRouter, GigaChat — опция; [ADR-008](../ADR-008-ai-provider.md)).

1. **ИИ-извлечение инсайтов** (фича «а») — реальный ИИ в разъёме `ParsedTable → BoardData`
   рядом с правилами (правила = fallback). Гибрид: ИИ выбирает колонки/график/нарратив,
   **числа считает код** из реальных данных. Только вошедшим; приватность (шлём схему+выборку,
   не весь файл); синхронно. Файлы: `lib/ai/*`, `lib/insight-engine/ai-plan.ts`+`extract.ts`,
   route `app/api/ai/extract`, проводка `LandingPage`/`ChatRail`.
2. **AI-чат по данным** (фича «б») — в Data-рейле ИИ отвечает словами по сводке доски +
   «Советник + Построить инсайт» (Apply строит новый инсайт на реальных числах через executePlan;
   правки существующего — руками, не чат). Чат хранится в `BoardData` (ADR-003); `sourceTable`
   в памяти (не персист). Файлы: `lib/ai/chat.ts`, route `app/api/ai/chat`, `lib/store.ts`, `ChatRail.tsx`.
3. **Donut `%`** (пункт 9c) — доля от целого (69%/31%) + центр TOTAL вместо `918000%`/«CHANNELS».

## Проверено в этой сессии (dev, OpenRouter, ключ в `.env`)
- Загрузка CSV вошедшим → ИИ строит 5 инсайтов разными графиками на реальных числах; нарратив → слайд.
- Чат: отвечает корректными числами; «Построй график …» → Apply → новый инсайт на холсте (6/6, Undo активен).
- Гейт сессии: гость → 401 на `/api/ai/extract` и `/api/ai/chat`.
- `tsc --noEmit` чист; `eslint` нового кода чист; `npm run build` проходит.

## Проверить ЗАВТРА (приёмка на свежую голову)
- [ ] Прогнать заново вживую: загрузить **свой** CSV и Excel (не только sales-sample) — инсайты адекватные? числа верные?
- [ ] Чат: задать 3–4 разных вопроса; проверить, что ответы по данным, а не выдуманы.
- [ ] Apply: построить пару новых инсайтов; убедиться, что цифры реальные и Undo откатывает.
- [ ] Donut: открыть пончик — доли + TOTAL, без `%`-мусора; проверить и на узком/широком масштабе.
- [ ] Сбой-путь: временно убрать `OPENROUTER_API_KEY` из `.env` → загрузка должна **тихо** падать на правила;
      чат должен показать «Не удалось получить ответ» (не молчать). Вернуть ключ.
- [ ] Перезагрузка проекта: после reload чат-история на месте (из доски), а «построить» недоступно до новой загрузки — ожидаемо.
- [ ] (Опц.) Глянуть, нормально ли читаются ответы/нарратив на EN-локали.

## Хвосты / на уборку (не горит)
- Тест-данные (dev/Neon): аккаунт `axon-ai-test-l05@example.com` + 3 тест-проекта; пробный CSV в `.playwright-mcp/`.
- Lint-долг: `prefer-const` в `ChartRenderer.tsx:842` и др. — предсуществующее, отдельной lint-уборкой.
- Решение OpenRouter ↔ GigaChat — отложено (агностичный слой; для прод-РФ вероятен GigaChat).

## Дальше по плану Урока 5
Шаг 1 (ИИ) закрыт. Опционально: uptime-мониторинг `axon-app.ru`, owner-админка. В конце — финальный
security-check (новые ИИ-поверхности: prompt-injection, секреты, лимиты). См. `docs/plan-lesson-05.md`.
