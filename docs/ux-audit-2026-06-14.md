# UX-аудит — Axon — 2026-06-14

Режим «по коду»: Axon — рабочее задеплоенное приложение (машина состояний, не роут-на-экран). Скан адаптирован: `src/` → `app/` + `lib/`. «Точки входа/выхода» проверяются не по URL, а по достижимости вью-состояний (store-действия + back/close).

## Сводка
- 🔴 Критических проблем: **0**
- 🟡 На доработку: **1** (мок-копирайт; 2 пункта мёртвого кода закрыты — см. ниже)
- Экранов (вью-состояний) покрыто: 16 (см. `docs/screens/`)

> **✅ Обновление 2026-06-14:** мёртвые `Presentation.tsx` и `ExpandedView.tsx` (строки таблицы ниже) **удалены**, tsc 0. Остался один 🟡 — мок-копирайт в BuildMode.

## 🔴 Критические (блокируют деплой)

Нет. Пустых обработчиков (`onClick={}` / `undefined`) — 0. Битых кнопок в живом продукте нет: совпадения grep'а — это `/storybook` (dev-витрина), мёртвые файлы и многострочные `<button>` (onClick на след. строке). Все вью-состояния достижимы и имеют выход (workspace → back на главную; drill-in → BackButton; модалки → Esc/крестик/клик-вне).

## 🟡 На доработку / на чистку

| Что | Где | Тип | Заметка |
|---|---|---|---|
| Мок-ответ AI «Full editing coming soon» показывается пользователю | `app/components/build/BuildMode.tsx:424` | заглушка-копирайт | Честная мок-реакция чата в Build-режиме (как `toast.info('в разработке')` из рецепта). Допустимо для прототипа; финальный копирайт — позже. |
| Мёртвый код `Presentation.tsx` (0 импортов) + хардкод-хексы | `app/components/Presentation.tsx` | dead code | Нигде не импортируется. Кандидат на удаление. Держит локальные хекс-цвета (не токены). |
| Мёртвый код `ExpandedView.tsx` (0 импортов) + хардкод-хексы | `app/components/ExpandedView.tsx` | dead code | Аналогично. Заменён живыми `canvas/InsightExpandedView.tsx` + `DataSetExpandedView.tsx`. |

> Удаление мёртвых файлов не делал без подтверждения (не я их создавал). Скажи «удали мёртвый код» — снесу `Presentation.tsx` и `ExpandedView.tsx` (оба 0 импортов, проверено grep'ом).

## Проверки responsive (Шаги 7–8, аудит по коду)
- ✅ Главная `/` — полный адаптив: nav сворачивается, hero-лесенка паддингов, dropzone адаптируется, сетка проектов `grid-cols-3 → 2 → 1`, контейнер кэпится `max-w-screen-xl` (на 1920px не расплывается).
- ✅ Воркспейс (Canvas/Слайды/Build/drill-in) — десктоп-first by design (как Figma/Miro). Ниже `lg` (1024px) показывается экран-заглушка `DesktopOnlyNotice` («Axon рассчитано на десктоп» + возврат к проектам) вместо «сжатого десктопа». Решение пользователя (2026-06-14).
- ✅ Фикс: dropzone переведена с хардкод-хексов на токены (`border-gold-500` / `border-border` / `bg-gold-500/…`).
- ⚠️ Живой Playwright-прогон не выполнялся (MCP-браузер занят активной сессией) — аудит на уровне кода. Пиксельную сверку скриншотами на 320/414/768/1024/1440/1920 стоит догнать, когда браузер освободится.

## Картинки (Шаг 10)
- ✅ 5 онбординг-PNG (1064×760, ~137–161 КБ) переведены на `next/image` → авто WebP/AVIF + адаптивный размер (`sizes`). Ручной `new Image()`-preload убран (грузил неоптимизированные оригиналы вдобавок).
- ✅ `next.config.ts`: `formats: avif/webp`, `deviceSizes` под брейкпоинты Axon, `minimumCacheTTL`.
- ℹ️ `sharp` присутствует (бандлится Next 15) — оптимизация активна. После правок `next.config` нужен рестарт dev-сервера.
- ℹ️ Тяжёлый пайплайн (`scripts/optimize-images.js` + git-хук) не заводил: Axon почти без растра (графики/иконки/спарклайны — SVG), 5 небольших PNG того не стоят. Неиспользуемые boilerplate-SVG (`next/vercel/window/globe/file.svg`) в `public/` — безвредны, можно удалить при желании.

## Вывод
Критических UX-проблем нет — по UX-гейту можно двигаться к Уроку 3 (деплой). Открытые пункты — косметика/чистка (🟡), не блокеры.
