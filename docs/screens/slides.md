# Экран: Slides (слайды) — режим presentation

**Где:** входной экран `/` → рабочая область, `store.mode = "presentation"` (это **состояние**, не отдельный URL).
**Доступ:** гость (без сохранения) + вошедший. Сохранение/шаринг — после входа.
Файлы: `components/presentation/SlideEditor.tsx` (центр-слайд + правый рельс `VisualizationStyleRail`),
лоток слайдов — `PresentationStructure.tsx`. **Двуязычный** (`SlideEditor` / `SlideTray`).

> **Сверено с кодом 2026-06-23 (Урок 7, Шаг 2).** Эта спека — результат рерайта слайдов (детальная);
> просмотр кода подтверждает: срезаны «движки» и архетипы Настроение/Список слов, тема deck-wide +
> свет/тьма, «+ Слайд»-пикер, «+ Новый дата-сет» снят. Формат подробный (не таблицы Шага 2) — оставлен
> намеренно, т.к. он полнее. Источник правды по экранам в целом — [`_global.md`](_global.md).
>
> Терминология: продукт собирает **презентацию** из **слайдов**. Слово «дека» не используем.
> Три режима воркспейса: **ХОЛСТ** (данные) · **СЛАЙДЫ** (сборка) · **ПОКАЗ** (вывод).

## Принцип: место = скоуп
Где стоит контрол — то он и меняет. Подписи «применяется ко всей презентации» не нужны.
- **Центр** — этот слайд (правится прямо на нём).
- **Правый рельс** — две секции: «ЭТОТ СЛАЙД» и «ВСЯ ПРЕЗЕНТАЦИЯ».
- **Низ** — лента слайдов (набор).

## Layout
- Слева — ChatRail (как в Canvas).
- **Центр — слайд, только редактируемый текст** (заголовок / сводка / заметки докладчика), правится inline по клику. **Контролов-кнопок на самом слайде нет.**
- **Справа — рельс из двух секций по скоупу** (виден, когда слайды есть):
  - **ЭТОТ СЛАЙД** (пер-слайдовое): один плоский дропдаун **«Как показать этот слайд»** (см. ниже).
  - **ВСЯ ПРЕЗЕНТАЦИЯ** (deck-wide): **Тема** (галерея пресетов + тумблер свет/тьма у Web-dashboard + «+ по образцу») и **Заметки** (тумблер вкл/выкл).
- **Снизу — лента слайдов** (горизонтальный скролл, drag-сортировка) + кнопка **«+ Слайд»**.

## Один плоский дропдаун «Как показать этот слайд»
Вместо двух меню (`Chart › …` на слайде + Формат) — **один плоский список без слов-категорий** «Вид/График». Каждая строка:
- **смысл крупно** (тёмный) + **техн. термин мелким серым** (напр. «Доли целого · Treemap»);
- archetype (`Chart`/`Big Number`/`Comparison`/`Quote`) программа **выводит сама** из выбора — пользователю это слово не показываем.

**Строки-графики** (archetype=`Chart` + chartType) — сверху, в порядке приоритета «красоты»; ниже разделителя — **не-графики**:

| Смысл | Термин (серым) | archetype / chartType |
|---|---|---|
| Доли целого | Treemap | Chart / Treemap |
| Доли целого | Donut | Chart / Donut |
| Сравнение | Lollipop | Chart / Lollipop |
| Связь | Scatter | Chart / Scatter |
| Профиль | Radar | Chart / Radar |
| Плотность | Heatmap | Chart / Heatmap |
| География | Map | Chart / Map |
| Количество | Dot Matrix | Chart / Dot Matrix |
| Динамика | Spline Area | Chart / Spline Area |
| Состав | Stacked Bar | Chart / Stacked Bar |
| *— разделитель —* | | |
| Одна цифра | — | Big Number |
| Сравнение цифр | — | Comparison |
| Цитата / текст | — | Quote |

- **Donut + Lollipop — высоко** (витрина приложения, самые красивые). **Spline Area и Stacked Bar — самый низ** (скучные стандартные; Spline выше Stacked Bar).
- **«Динамику» покрасивее** решает не новый тип графика, а **приоритет ИИ-подбора** (см. ниже) — для временны́х данных ИИ выбирает яркий тип, Spline/Stacked Bar — запасные.
- **Архетипы `Настроение`/`Список слов` убраны** (самые непонятные на тестах).

**ИИ-первый проход:** при создании слайда ИИ сам ставит archetype и тип графика. Пользователь ничего не выбирает на старте — дропдаун нужен только чтобы **переделать** (тихий override). Приоритет ИИ-подбора смещён к ярким типам; Spline Area / Stacked Bar — самые низкоприоритетные. *(Логика подбора — отдельный мелкий шаг, не часть UI-рельса.)*

## Тема (deck-wide)
- **Галерея пресетов** `PRESENTATION_THEMES` — тык по плитке меняет оформление **всей презентации** (шрифты/цвет/радиус через `--slide-*`). Пресеты: Editorial, Soft, **Web-dashboard** (+ ещё Bold / Minimal / Magazine — добавляются).
- **Web-dashboard — тумблер свет/тьма** внутри одной темы (для веб-вывода; согласовано с «Показом → Interactive · dashboard»).
- **«+ по образцу»** — отдельная выделенная кнопка: загрузка картинки (Pinterest) → vision-модель → новая Theme. **Реализация позже, AI-зависимая** (Ярус 3, отдельной сессией после Урока 6) — см. `backlog.md`.

## Данные
Слайды собираются из дата-сетов проекта. Тема презентации (`presentationThemeId`) — состояние проекта.
Слайд рождается из существующего датасета (`addSlideWithDataSet`); новые данные заводятся **на ХОЛСТЕ** через «+ Добавить файл» (файл → ИИ → датасеты). Пустых узлов не создаём.

## Состояния
- **default:** слайд (текст) + рельс из двух секций + лента слайдов.
- **empty:** слайдов нет (`slideOrder` пуст) — подсказка «добавь слайд из данных холста» / «загрузи файл на ХОЛСТЕ»; рельс свёрнут/скрыт.
- **loading/error:** наследуются от загрузки проекта.

## Действия
- **Inline-правка текста** слайда: заголовок, сводка, заметки докладчика — клик, автосейв.
- **Вид / График** (правый рельс, секция «ЭТОТ СЛАЙД») — меняет **только текущий слайд**. Сразу, автосейв.
- **Тема** (правый рельс, секция «ВСЯ ПРЕЗЕНТАЦИЯ») — меняет **всю презентацию**. Сразу, автосейв.
- **Заметки** (тумблер вкл/выкл, в рельсе «ВСЯ ПРЕЗЕНТАЦИЯ») — показывать ли блок заметок докладчика. (Voiceover-режим убран в Шаге 3.)
- **Лента:** drag-сортировка слайдов; **«+ Слайд»** → пикер датасетов холста, которых ещё **нет** в презентации → выбрал → стал слайдом; **удалить слайд** — крестик на плитке, **с подтверждением** (`<AlertDialog>`).
- text-инсайт, привязанный к датасету слайда, авто-переключает Вид на **Цитата** (видимо пользователю).

## Что убрано из прежнего экрана
- ~~Рейл «Visualization Style» с движками **SciChart / Highcharts / D3**~~ — снесено целиком (фейковый выбор «движка», имена библиотек наружу).
- ~~Combo-кнопка «Формат» (`Chart › Stacked Bar`) на самом слайде~~ — управление видом ушло в правый рельс.
- ~~Блок «Настройки подачи»: **Audience / Tone**~~ — снесены (по коду кормили только запасную захардкоженную строку, перекрытую summary → «не работали»).
- ~~Дублирующий `SLIDE FORMAT`~~ — был вторым редактором того же поля `archetype`, удалён.
- ~~«+ Новый дата-сет»~~ (плодил пустой узел-тупик) — данные заводятся файлом на ХОЛСТЕ.

## Связи
Переключение режима ↔ [Canvas](canvas.md) / [Present](present.md). Drill-in [dataset](dataset-drill-in.md) доступен.

## Доступы
Как Canvas: гость работает, сохранение/шаринг — после входа.

## Визуал

### Layout Desktop
- Оболочка `h-screen`, `bg-canvas`. Слева ChatRail (как в Canvas).
- Центр — **слайд** (только inline-текст). Снизу — **лента слайдов** (горизонтальный скролл `.slide-scroll`, drag-сортировка) + «+ Слайд».
- Справа — full-height **рельс из двух секций** (виден, когда `slideOrder` не пуст): «ЭТОТ СЛАЙД» (Вид/График) и «ВСЯ ПРЕЗЕНТАЦИЯ» (Тема/Заметки).
- Все цвета/шрифты — через токены `--slide-*` (тема) и DESIGN.md → UIKit. Хардкод хексов/пикселей на экране запрещён.

### Layout Mobile (≤640px)
- ChatRail скрыт; правый рельс → сворачивается/уходит вниз; лента слайдов — горизонтальный скролл. **TODO:** мобильный layout.

### Компоненты (сверху вниз)
- **ChatRail** — как в Canvas.
- **Слайд** — превью слайда; текстовые зоны редактируются inline.
- **Правый рельс** — две секции; дропдауны Вид/График и плитки тем (паттерн `ChartTypeDropdown`: `surface-raised` + border).
- **Лента слайдов** — карточки-превью, drag-сортировка; «+ Слайд» (пикер) в конце.
- **Удаление слайда** → `<AlertDialog>` подтверждение (есть в ките).

### Визуальные состояния
- **default:** слайд + рельс + лента.
- **empty:** `slideOrder` пуст — подсказка добавить слайд / загрузить файл; рельс скрыт.
- **loading/error:** наследуются от загрузки проекта.

### Edge cases визуально
- Много слайдов — горизонтальный скролл ленты.
- Длинная сводка / заметки — wrap в превью.
- Невидимый текст сводки на тёмной теме — **фикс в рамках рерайта** (хардкод фона `#FAF8F2` → `var(--slide-muted)`).
- Кнопка «Сохранить» не должна лезть на правый рельс — своё место в тулбаре.

### Открытые вопросы / TODO
- ~~Voiceover~~ убран (Шаг 3); ~~Sentiment/Word List~~ срезаны (Шаг 4a); ~~подписи плиток~~ решены — «смысл крупно + термин серым», один плоский список; ~~язык подписей режимов~~ — Холст/Слайды/Показ (Шаг 4f).
- ~~Схлопывание полей модели `Slide`~~ — **Шаг 6 ✅ сделано**: пер-слайдовые `visualStyle/showLabels/showGrid/stackedBars` удалены, оформление только через тему (deck-wide). Старые доски/`/p/[token]` совместимы (лишние ключи игнорируются, тема → фоллбэк `editorial`).
- TODO: мобильный layout.
