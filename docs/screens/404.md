# Экран: 404 — Не найдено

**URL:** любой несуществующий путь.
**Доступ:** все.

> **В коде кастомной страницы нет** — используется **стандартная страница Next.js** (дефолтный
> `not-found`). Своего `not-found.tsx` в `apps/app` пока не заведено.

---

## Состояния экрана

| Состояние | Условие |
|---|---|
| default | стандартный экран Next «404 — This page could not be found» |

## Элементы интерфейса

### (стандартная страница Next)
- Текст «404» + краткое пояснение. Кастомных кнопок/ссылок нет.

## Сообщения

| Ситуация | Тип | Текст сообщения |
|---|---|---|
| Путь не найден | — | стандартный текст Next (404) |
| `/admin/*` без прав админа | — | сюда же ведёт `notFound()` (скрываем существование админки) |

## Автоматика и уведомления

| Триггер | Канал | Получатель | Текст / Действие |
|---|---|---|---|
| — | — | — | нет |

## Валидация полей

| Поле | Правило | Сообщение об ошибке |
|---|---|---|
| — | — | — |

---

## Открытые вопросы / находки
- **Кастомной 404 нет.** Намерение (из старой спеки): свой экран в стиле бренда — крупная «404»
  (serif), текст, кнопка «На главную» → `/`. Сделать `apps/app/app/not-found.tsx`. → backlog/полировка.
- `/admin/*` без прав отдаёт именно 404 (через `notFound()`), отдельной 403 нет — осознанно.
