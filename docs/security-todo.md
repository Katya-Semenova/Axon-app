# Security TODO — отложенные проверки и доработки

> Находки само-ревью по рецепту `/new-feature` (роль «ревью безопасности»).
> Закрываем по плану урока — на указанных шагах.

## Из Шага 5 (auth)
- 🟡 **IDOR / изоляция досок → Шаг 7.** Серверные экшены `getBoard(id)` / `saveBoard(id)`
  (`app/actions/board.ts`) пока НЕ проверяют, что доска принадлежит вошедшему
  пользователю. Сейчас НЕ эксплуатируется (одна общая демо-доска, приватных данных
  нет), но при разведении досок по пользователям **обязательно** добавить проверку
  `Board.ownerId === session.userId` (чужая доска → 404).
- 🟡 **User-enumeration → Шаг 9.** Сообщения об ошибке входа/регистрации могут выдавать,
  занят ли email. Унифицировать/смягчить.
- 🟡 **Защита входа → Шаг 9.** Rate-limit на `/api/auth/*`, флаги cookie
  (HttpOnly / Secure / SameSite), лимит попыток подбора.
- 🟢 Мелочь: в `BoardSync` сид-сохранение пустой доски для только что вошедшего может не
  сработать с первого раза (сессия ещё «pending») — самокорректируется при следующем изменении.

## Общее
- Полный security-check всего бэкенда (auth/IDOR/инъекции/сессии) — **Шаг 13**.
