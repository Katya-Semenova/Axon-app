# Тест-кейсы (BDD) — критичные пути Axon

> Сценарии «Дано → Когда → Тогда» на главные пути сервиса. Проходимы вручную в браузере,
> позже превращаются в автотесты (e2e Playwright + интеграционные Vitest). Формат **black-box**:
> описываем поведение снаружи (UI/API), не внутренние функции. Урок 7, Шаг 4 (2026-06-23).
>
> Маршруты сервиса — под `basePath /ai-studio`. Источник правды по экранам — [`screens/`](screens/)
> и [`screens/_global.md`](screens/_global.md).

**Статусы прогона:** ✅ проверено · 🟡 частично/расхождение (см. backlog) · ⏳ проверить на проде.
**Автотесты (Урок 7, Задание 5.1):** часть сценария 5 («файл разбирается → инсайты/дата-сеты»)
закрыта Vitest-тестами `development/apps/app/tests/` (29 шт., включая регрессии ревью Урока 4:
усечение CSV, евро-числа, сортировка ряда, «918000%», формульные инъекции). **Playwright e2e
(`tests/e2e/`, прод-сборка + тестовая БД axon_test):** сценарий 1 (регистрация + cookie HttpOnly +
/settings после входа), сценарий 2 (единая ошибка входа — сравниваются оба текста), сценарий 4
(гость: /settings→login, админка→404). Гейты: pre-push (Vitest) и деплой (test:all) —
`docs/automations.md`. Осталось на e2e: сценарий 3 (IDOR), 5 (загрузка файла через UI), 6 (шаринг),
429 rate-limit.
**Оговорка:** локально приложение не запускаем (Mac не тянет dev) — статусы ниже по аудитам и живым
проверкам прода (`docs/audits/*`, журнал сессий), либо помечены ⏳.

---

## Сценарий 1: Регистрация и первый вход
- **Дано:** пользователь не зарегистрирован, открыт `/register`
- **Когда:** заполняет имя, валидный email, пароль ≥ 8, ставит галочку согласия, жмёт «Зарегистрироваться»
- **Тогда:** в БД создаётся `User` с захешированным паролем (не plaintext)
- **Тогда:** редирект на `/` (вход в приложение)
- **Тогда:** cookie сессии — `HttpOnly` (+ `Secure` на проде), ставит Better Auth
- **Тогда:** в шапке вместо «Войти» — аватар (AuthNav)
- **Статус:** ✅ **проверено вживую 2026-06-24** (свежая регистрация на проде: создан аккаунт, редирект в
  приложение, аватар вместо «Войти»). Флаги cookie сверены в DevTools: кука входа `__Secure-better-auth.session_token`
  — `HttpOnly` ✓, `Secure` ✓, `SameSite=Lax`, имя с префиксом `__Secure-`. Ранее — живые проверки 2026-06-19/22.

## Сценарий 2: Вход с неверным паролем — единый ответ + защита от перебора
- **Дано:** пользователь зарегистрирован
- **Когда:** пытается войти с неверным паролем
- **Тогда:** ошибка «Неверный email или пароль» — **такая же**, как для несуществующего email (защита от перебора аккаунтов)
- **Тогда:** после серии неудачных попыток с одного IP — `429 Too many requests` (rate limit на `/api/auth/*`)
- **Статус:** ✅ единый ответ на входе — **подтверждён вживую 2026-06-24** (неверный пароль → общая «Invalid
  email or password», не раскрывает существование аккаунта). 🟡 текст ошибки по-английски (i18n auth-экранов — в
  backlog). 🟡 rate limit включён, но мягче рекомендации урока (сейчас 10/60s, советуют 5/мин — в backlog).
  🟡 **регистрация** при занятом email отвечает «email занят» (раскрывает аккаунт — принятый компромисс, в backlog).

## Сценарий 3: IDOR закрыт (чужие проекты недоступны)
- **Дано:** два вошедших пользователя A и B; у каждого есть свой проект (доска)
- **Когда:** A пытается открыть проект B по его id (через server-action `getBoard`)
- **Тогда:** данные B **не возвращаются** (проверка `Board.ownerId === userId` → как «не найдено»), не «Forbidden» (это утечка факта существования)
- **Когда:** A пытается изменить/удалить проект B
- **Тогда:** действие отклонено, проект B в БД цел
- **Статус:** ✅ проверено на 2 аккаунтах — `docs/audits/security-check-2026-06-17.md`.

## Сценарий 4: Защита страниц (гейты доступа)
- **Дано:** пользователь не авторизован
- **Когда:** открывает `/settings` напрямую по URL
- **Тогда:** серверный редирект на `/login`
- **Когда:** не-админ открывает `/admin/users`
- **Тогда:** `404` (скрываем существование админки), не редирект
- **Когда:** не-админ открывает `/storybook` на проде (Урок 7, Задание 12)
- **Тогда:** `404` (витрина дизайн-системы — только админу; в dev открыта)
- **Когда:** пользователь успешно входит и снова открывает `/settings`
- **Тогда:** страница настроек доступна
- **Статус:** ✅ **проверено вживую 2026-06-23** (HTTP-запросы к проду, без входа): `/ai-studio/admin/users` → `404`; `/ai-studio/settings` → редирект на `/ai-studio/login`. + гейты по коду (`settings/page.tsx`, `lib/admin.ts`). **Доступ к `/settings` ПОСЛЕ входа — ✅ подтверждён вживую 2026-06-24** (настройки открылись, смена пароля и загрузка аватара отработали).

## Сценарий 5: Главный флоу — файл → инсайты → сохранение → персист
- **Дано:** вошедший пользователь на входном экране `/`
- **Когда:** перетаскивает CSV/Excel в dropzone
- **Тогда:** файл разбирается, на холсте появляются инсайты и дата-сеты (графики)
- **Тогда:** создаётся проект пользователя; он виден в «Мои проекты»
- **Тогда:** после перезагрузки страницы проект и его содержимое на месте (сохранены в БД)
- **Статус:** ✅ смоук + живая проверка (загрузка → инсайты → сохранение → авто-пан) — `docs/audits/persistence-check-2026-06-17.md` + прод 2026-06-19/22. **Свежий прогон вживую 2026-06-24:** вошедшая бросила Excel из `test-data/` → графики на холсте → у вошедшего проект создаётся сам (без кнопки «Save», `createProjectFromData`) → после жёсткой перезагрузки (Ctrl+Shift+R) проект на месте в «Мои проекты» = **персист подтверждён**.

## Сценарий 6: Публичная презентация по ссылке (share by token)
- **Дано:** владелец собрал деку и в «Показе» создал публичную ссылку `/p/[token]`
- **Когда:** получатель (без входа) открывает ссылку
- **Тогда:** видит деку read-only: листание стрелками/точками/клавишами, без возможности правки
- **Когда:** владелец отзывает ссылку, получатель открывает её снова
- **Тогда:** «Презентация недоступна» (токен невалиден → `deck=null`)
- **Статус:** ✅ шаринг по ссылке + отзыв (Шаг 12). **Проверено вживую 2026-06-23:** битый токен
  `/ai-studio/p/<неверный>` → «Презентация недоступна». **Создание/отзыв ссылки вошедшим — ✅ проверено вживую
  2026-06-24:** владелец создал публичную ссылку → открыл в инкогнито (виден read-only) → нажал «Отозвать ссылку»
  → в инкогнито обновил → «Презентация недоступна».

---

## Итог прогона

### Прогон ПОД ВХОДОМ — вживую на проде 2026-06-24 (пользователь)
- ✅ **Сценарий 1** — свежая регистрация + вход; флаги cookie входа `HttpOnly`/`Secure`/`SameSite=Lax` сверены в DevTools.
- ✅ **Сценарий 2** — неверный пароль → общая «Invalid email or password» (не раскрывает аккаунт). *(текст по-EN — i18n в backlog.)*
- ✅ **Сценарий 4** — `/settings` ПОСЛЕ входа открывается (смена пароля + загрузка аватара работают).
- ✅ **Сценарий 5** — Excel → графики → авто-создание проекта у вошедшего → персист после Ctrl+Shift+R.
- ✅ **Сценарий 6** — публичная ссылка: создание → просмотр в инкогнито → «Отозвать ссылку» → «Презентация недоступна».
- Доп.: гостевой «Save» (Sign up & save / Sign in & save) — работает.
- 🟡 По пути — UX-находки (все в `backlog.md`): нижняя ссылка гостя ведёт на `/login` (двойной прыжок до
  регистрации); на холсте нет аватара/кабинета → путаница Save vs Sign in; в модалке «Save» нет согласия;
  автозаполнение не работает в модалке «Save».

### Прогон БЕЗ входа — вживую 2026-06-23 (HTTP к проду)
- ✅ Сценарий 4 (гость): админка → 404; настройки гостю → редирект на /login.
- ✅ Сценарий 6 (гость): битый токен публичной ссылки → «Презентация недоступна».

### Подтверждено ранее (аудиты)
- ✅ **Сценарий 3 (IDOR)** — на 2 аккаунтах, `security-check-2026-06-17.md`. Вручную в браузере не воспроизводим —
  закрывается автотестами на финале курса.
- 🟡 Сценарий 2 — расхождения с рекомендациями (rate limit 10/60s vs 5/мин; регистрация раскрывает занятый
  email) — в backlog, не критично. 429 по rate-limit вручную не гоняли (риск залочить себе вход) — на автотесты.

**Итог:** красных (сломано) расхождений нет. Все 6 критичных сценариев проверены — 1/2/4/5/6 живьём под входом
2026-06-24, 3 (IDOR) и 429 — на автотесты финала. **Шаг 4 закрыт.**

> Расхождений уровня 🔴 (сломано) нет. Авторизованные сценарии корректнее закрывать **автотестами
> на тестовой БД** (e2e Playwright + интеграционные Vitest, в гейте push/deploy) — финальный этап
> курса; ручной прод-прогон под входом — как промежуточная проверка, делает пользователь.
